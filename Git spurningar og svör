Git spurningar og svör

1. Hvað gera eftirfarandi Linux skipanir?
	cd  = fara í aðra möppu
	cd ~ = fara í heima svæðið
	ls = prenta út "allt" sem er í möppunni
	pwd = prenta hvar þú ert núna
	mkdir = býr til möppu


2. Hvað gera eftirfarandi git skipanir og hvers vegna eru þær gagnlegar?
	git clone = copy-ar repository 
	git log  = sýnir commit history
	git status = sýnir stöðuna þýna
	git diff = sýnir munin á milli commits
	git checkout = breytir um "branch"

3. Hver er munurinn á eftirfarandi git skipunum:
	a) git diff = sýnir mun á milli commits
	b) git diff --staged = sýnir mun á milli commits sem eru í "staged" svæðinu
	c) git diff commit1 commit2 = sjá munin á milli 2 commits

4. 	Hvað er útgáfustýring (e.version control)?
	Það getur verið einhverskonar hugbúnaður sem heldur utan um breytingu á skjölum, git er eitt dæmi um það.

5.	Hverjir eru helstu kostir við að nota GIT?
	Það getur verið gott að nota git út af mörgum ástæðum, ein sú helsta er sú að git getur auðveldlega búið til nýjar greinar og það er auðvelt að notast við það að gera það. svo er líka það að það er auðvelt að skipta vinnu á git því að hver einstaklingur getur unnið í sínu repository.

6.	Hversu oft telur þú að eigi að gera commit í verkefni, rökstuddu?
	því meira því betra, en ekki ofgera það samt alveg því að það mun lengja vinnutímann þinn verulega. Það er gott að gera mörg commits því að þá getur þú farið aftur í efnið og breytt því, og því fleiri commits sem þú gerir því auðveldara er að finna efnið og breyta því