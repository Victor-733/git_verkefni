Git spurningar og svör

1. Hvað gera eftirfarandi Linux skipanir?
	cd  = fara í aðra möppu
	cd ~ = fara í heima svæðið
	ls = prenta út "allt" sem er í möppunni
	pwd = prenta hvar þú ert núna
	mkdir = býr til möppu


2. Hvað gera eftirfarandi git skipanir og hvers vegna eru þær gagnlegar?
	git clone = copy-ar repository 
	git log  = sýnir commit history
	git status = sýnir stöðuna þýna
	git diff = sýnir munin á milli commits
	git checkout = breytir um "branch"

3. Hver er munurinn á eftirfarandi git skipunum:
	a) git diff = sýnir mun á milli commits
	b) git diff --staged = sýnir mun á milli commits sem eru í "staged" svæðinu
	c) git diff commit1 commit2 = sjá munin á milli 2 commits